import { html } from "foldkit/html"

import { Message } from "./main"

export const {
  div,
  button,
  span,
  p,
  h1,
  h2,
  h3,
  input,
  label,
  Class,
  OnClick,
  OnInput,
  Type,
  Value,
} = html<Message>()
